[project]
name = "initial_project"
version = "0.1.0"
description = "initial_project using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.13"
dependencies = [
    "crewai[tools]>=0.102.0,<1.0.0"
]

[project.scripts]
initial_project = "initial_project.main:run"
run_crew = "initial_project.main:run"
train = "initial_project.main:train"
replay = "initial_project.main:replay"
test = "initial_project.main:test"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
